pipeline{

    agent {
        lable 'slave1'
    }

    stages {

        stage('STAGE1') {
            step {
                sh '''
                pwd
                ls -a
                '''
            }
        }

        stage('stage2') {
            step {
               sh ' echo "this is stage 2" '
            }
        }

    }
}