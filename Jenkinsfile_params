pipeline{

    agent any

    parameters {
       
        string defaultValue: 'Branch', description: 'This is from the dlecraive generator', name: 'Sneha'
        choice choices: ['TEST', 'QA', 'PROD'], description: 'Choose the env to deploy', name: 'Environement'
        booleanParam description: 'Choose the boolean value', name: 'Chooose Boolean '
        
    }

    stages {

        stage ('QA') {
            steps {
                script {
                echo "THIS IS QA STAGE"
                }
            }
        }

        stage ('TEST') {
            steps {
                script{
                echo "THis is test stage"
                }
            }
        }
        stage ('PROD') {
            steps{
                script{
            echo "this is prod stage"
                }
            }
        }
    }

}